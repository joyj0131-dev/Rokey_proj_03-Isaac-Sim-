# p95(95백분위수)와 다중프레임 융합 — M5 정확도 관문의 원리

> 작성: 2026-07-24. 배경: ArUco 마커 측위 M5 정확도 관문(스펙 부록 A/C, 계획 Phase 1).

## 1. 백분위수(percentile)란

측정값을 **작은 것부터 큰 순서로 줄 세웠을 때, "아래에서 몇 %" 지점의 값**이다.

- **중앙값(median, p50)**: 아래에서 50% 지점. 절반은 이보다 작고 절반은 크다.
- **p95(95백분위수)**: 아래에서 95% 지점. **측정의 95%가 이 값보다 작거나 같다.** 나쁜 쪽 5%만 이보다 크다.
- **최대(max, p100)**: 제일 나쁜 하나.

## 2. M5 실측으로 본 p95

M5 는 로봇을 마커 앞 34개 자세에 세워 "마커로 복원한 내 위치"와 GT(정답)의 오차를 쟀다.
그 34개 오차를 정렬하면:

```
[ 0.6cm, 0.8cm, ...,  1.40cm , ...,  4.15cm ,  5.88cm ]
                        ↑median(p50)   ↑p95        ↑max
  ← 절반은 여기 아래 →            ← 95%는 여기 아래 →   ← 최악 1개
```

- **중앙값 1.40cm**: 자세의 절반은 1.40cm보다 정확했다.
- **p95 = 4.15cm**: 자세의 95%(34개 중 약 32개)가 4.15cm 안. 나쁜 쪽 5%(약 2개)만 그보다 컸다.
- **max 5.88cm**: 제일 나쁜 딱 하나.

yaw 도 같은 방식: 중앙값 0.42°, p95 1.68°, max 1.77°.

## 3. 왜 관문 기준을 median 이 아니라 p95 로 쓰나

| 지표 | 성격 | 관문으로서의 문제 |
|---|---|---|
| max | 이상치 한 방에 휘둘림 | 너무 가혹(운 나쁜 1개로 전체 탈락) |
| median | "보통 얼마나 정확한가" | 너무 관대(가끔 크게 틀리는 **꼬리**를 못 잡음) |
| **p95** | "거의 항상(95%) 이 정도 안" | **꼬리를 걸러내면서 이상치엔 안 휘둘림 → 균형** |

측위는 **가끔 크게 틀리는 것이 위험**하다. 로봇이 5%의 확률로 5cm 빗나가면 그 순간 도킹·정렬을
놓칠 수 있다. 그래서 "평균적으로 정확"(median)이 아니라 "거의 항상 정확"(p95)을 보장하도록
p95 를 관문으로 쓴다. M5 목표는 **위치 p95 ≤ 2cm, yaw p95 ≤ 1°**.

## 4. M5 결과의 진짜 문제 = "꼬리"

- 대부분 자세는 이미 합격선 안(중앙값 1.4cm).
- 그런데 **먼 거리(1.9m) + 비스듬한 각도(8°)** 조합에서 4~6cm로 튄다.
- 오차는 **횡방향(월드 Z, 표준편차 19mm)** 이 지배한다. 평면 마커를 비스듬히 볼수록 마커의
  평면내 회전·횡위치가 관측이 약해지는 기하 한계다(깊이/월드 X 는 7mm로 잘 관측됨).

즉 문제는 "평균 정확도"가 아니라 **꼬리(가끔 크게 틀림)** 다.

## 5. 왜 다중프레임 융합이 이 꼬리를 깎나

한 마커를 **여러 프레임(K장) 보고 그 측위 추정을 평균/필터**한다.

**원리 — 랜덤 노이즈는 평균하면 √K 배로 준다.** 각 프레임의 측위 오차에 **평균이 0인 랜덤
성분**(검출 코너 지터, 렌더 샘플링 노이즈 등)이 섞여 있으면, K장을 평균할 때 그 성분의
표준편차가 `σ/√K` 로 줄어든다. 예: 횡 노이즈 σ=19mm 를 K=10 프레임 평균하면 `19/√10 ≈ 6mm`
→ 튀던 꼬리가 크게 눌려 p95 가 2cm 아래로 내려갈 여지가 생긴다.

**전제와 한계(중요)**: 평균이 듣는 것은 **프레임마다 값이 흔들리는(랜덤) 성분**일 때다.
만약 어떤 자세의 오차가 **매 프레임 똑같은 고정 편향(bias)** 이면(정지 상태의 결정론적 렌더에서
그럴 수 있다) 평균해도 그대로다. 그 경우 진짜 해법은 **로봇이 움직이며 서로 다른 시점에서
본 관측을 오도메트리로 묶어 융합**하는 것이다(마커+휠 오도 상보필터, 스펙의 Phase 3 융합).
그래서 M5 의 다중프레임 구현은 **실측으로 평균 효과가 실제로 나오는지**를 함께 보고한다 —
효과가 없으면 정직하게 보고하고 오도메트리 융합으로 넘어간다.

## 6. 한 줄 요약

- **p95** = "측정의 95%가 이 값 안" = 가끔 크게 틀리는 꼬리까지 잡는 near-worst-case 기준.
- M5 는 median 1.4cm 로 보통은 좋지만 **꼬리가 p95 4.15cm** 로 목표(2cm)를 넘긴다.
- **다중프레임 융합**은 프레임별 랜덤 노이즈를 √K 로 눌러 그 꼬리를 깎는다(랜덤 성분에 한함).
