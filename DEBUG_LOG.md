# 디버깅 로그

프로젝트 진행 중 막혔던 것과 해결 방법을 날짜별로 굵직하게만 기록한다.
프로젝트의 현재 상태는 `HANDOFF.md`를 본다.

---

## 2026-07-19

### CPU PhysX 전제가 틀렸음 → GPU 전환
- **증상**: 문서에 "CUDA 없어 CPU PhysX로 실행"이라 적혀 있어 그대로 따르고 있었음.
- **원인**: 그건 다른 환경(Codex 헤드리스, CUDA 없음)의 기록. 이 머신은 RTX 5080이라 해당 없음.
- **해결**: 물리 씬을 GPU로 전환(`BroadphaseType="GPU"`, `EnableGPUDynamics=True`).
  리프트 테스트는 계속 통과하되 접촉 해석 차이로 뒷축 상승이 0.048 → 0.031 m로 감소(기준은 만족).

### 메카넘 제자리 회전 각속도가 명령과 안 맞음
- **증상**: `wz=0.5` 명령에 실제 0.67 rad/s(약 1.34배).
- **원인**: 제자리 회전이 **롤러 슬립에 지배**됨. 게인을 낮췄더니 오히려 회전이 커지는 **비단조** 특성
  (scale 1.0 → 115°, scale 0.745 → 175°).
- **해결**: 기하학적 계산 대신 동작점(wz≈0.5)에서 실측 피팅. `YAW_SCALE=1.12` → 89.9°/3s(≈0.52 rad/s).
  전진·strafe는 원래 1:1이라 영향 없음.

### flatten 에셋 검증이 오탐으로 실패 표시
- **증상**: 파일은 정상 생성됐는데 `BAKE_OK=False`.
- **원인**: 검증 로직이 과하게 엄격. `GetCompositionAssetDependencies()`가 빈 문자열 `['']`을 반환하는데
  이를 외부 의존성으로 오판, 지오메트리 체크를 `Mesh/Cube`만 해서 실제 형상인 `Cylinder/Sphere`를 못 잡음.
- **해결**: 빈 문자열 필터링 + `UsdGeom.Gprim`으로 체크 → `SELF_CONTAINED=True`, 지오메트리 40개 확인.

---

## 2026-07-19 ~ 07-20

### 좀비 Isaac 프로세스가 이후 모든 실패의 근본 원인
- **증상**: 전체가 느려지고, ROS 2 브리지 기동이 반복 실패.
- **원인**: Isaac Sim은 **`app.close()`가 걸려 프로세스가 종료되지 않는 경우**가 있다. 에셋 빌드 3개가
  결과(`BAKE_OK=True`)를 찍고도 3~4시간 살아남아 GPU/RAM을 점유. 여러 kit 프로세스가 동시에 뜨면서
  `another kit process is locking it` 충돌 발생.
- **해결**: 좀비 종료 후 정상 동작. **교훈: 로그에 "완료"가 찍힌 것은 프로세스가 죽었다는 뜻이 아니다.**
  실행 전후로 프로세스를 확인하고, 새 Isaac 실행 전 기존 프로세스를 정리할 것.

### ROS 2 브리지가 시작되지 않음 (Python 3.11 vs 3.10)
- **증상**: `Could not import system rclpy` → `librmw_fastrtps_cpp.so` 로드 실패 →
  `OmniGraphError: Could not create node 'isaacsim.ros2.bridge.ROS2SubscribeTwist'`.
- **원인**: Isaac 터미널에 시스템 ROS 2 Humble(Python 3.10)이 소싱돼 있으면, 브리지가 3.10용
  rclpy/rmw를 잡으려다 Isaac의 3.11과 안 맞아 실패.
- **해결**: Isaac 쪽은 `/opt/ros`를 **소싱하지 않고**, 내부 Humble 라이브러리
  (`exts/isaacsim.ros2.bridge/humble/lib`)를 `LD_LIBRARY_PATH`에 추가. 런처가 이를 처리.
  (반대로 경로만 제거하고 내부 libs를 안 넣으면 똑같이 rmw 로드 실패한다.)

### /cmd_vel을 발행해도 로봇이 안 움직임
- **원인**: 도메인 ID와 FastDDS 화이트리스트가 양쪽에서 일치하지 않으면 DDS 디스커버리가 안 됨.
- **해결**: 양쪽 모두 `ROS_DOMAIN_ID=126`, `RMW_IMPLEMENTATION=rmw_fastrtps_cpp`,
  같은 `~/.ros/fastdds_whitelist.xml` 사용. (화이트리스트에 `127.0.0.1`이 있어 같은 머신 검증도 가능)

### 런처 실행 권한 누락
- **증상**: `run_mecanum_ros2_drive.sh: 허가 거부`로 드라이버가 아예 안 뜸.
- **해결**: `chmod +x`.

---

## 2026-07-20

### 텔레옵에서 방향키만 동작 안 함 (회전·팔은 정상)
- **증상**: `a/d`(회전), `o/c`(팔)는 되는데 방향키로 이동만 안 됨. 오류 로그 없음.
- **원인**: `sys.stdin.read(1)`이 파이썬 내부 버퍼를 사용. 방향키는 3바이트 이스케이프 시퀀스(`\x1b[A`)라
  read(1)이 3바이트를 통째로 버퍼에 당겨오고 `\x1b`만 반환 → 이후 `select`는 OS fd에 데이터가 없다고
  보고 나머지 `[A`를 못 읽음. 단일 문자 키는 이 문제가 없어 정상 동작했던 것.
- **해결**: 버퍼를 타지 않는 `os.read(fd, n)`으로 교체. pty로 방향키/단일키 전부 파싱 검증 완료.

---

## 2026-07-20 (오후) — 팀원 주차장 에셋 교체 후 통합

`parking/` 폴더를 팀원이 만든 버전으로 통째 교체하면서 생긴 문제들.

### 차량 12대의 물리가 통째로 사라짐
- **증상**: `build_integrated_parking_field.py` 실행 시
  `FAB 차량 물리 구동계가 누락됐습니다: /World/ParkingVehicles/Parked/A3_Compact`.
- **오진 경로**: 처음엔 A3_Compact만의 문제로 보였으나 12대 전부 동일했다. 순회 첫 번째였을 뿐.
- **원인**: `parking_environment.usd`가 **절대경로** `/home/rokey/Isaac_envo/fab_vehicles.usd`를
  참조. 팀원 머신의 디렉터리 배치가 그대로 구워진 것이고, 이 머신엔 그 경로가 없다.
  참조가 실패하니 차량은 로컬의 `PhysicsRigidBodyAPI` 하나만 남고 지오메트리·바퀴·질량이 전부 유실.
  같은 경로로 `/World/PhysicsScene`, `/World/VehiclePhysics`, `/World/Looks/FabColors`도 함께 죽어 있었다.
- **판별법**: 스키마 조회보다 **합성된 자식 수**가 확실하다. `A3_Compact`의 자식이 0개
  (정상이면 바디+바퀴4+섀시 = 6개)인 것이 플러그인과 무관한 결정적 증거.
- **해결**: `build_parking_environment.py`의 참조 4곳을 상대경로 `../fab_vehicles.usd`로 전환하고,
  존재하지 않는 경로를 가리키던 폴백 상수 `Path("/home/rokey/Isaac_envo")`를 제거(없으면 즉시 실패하도록).
- **교훈**: USD에 절대경로를 구우면 트리를 옮기는 순간 전부 끊긴다. 팀 간 에셋 교환에서는 특히 위험하다.

### 진단 도구가 위음성을 냄 (standalone pxr의 함정)
- **증상**: `fab_vehicles.usd` 원본까지 `PhysxVehicleAPI`가 없다고 나와 "원본도 깨졌다"고 오판.
- **원인**: SimulationApp 없이 `omni.usd.libs`의 pxr만 쓰면 **omni.physx 스키마가 등록되지 않아**
  `Usd.Stage.Open()` 후 `GetAppliedSchemas()`가 모르는 API를 조용히 걸러낸다.
  `UsdPhysics` 코어 스키마는 보이고 `PhysxSchema`만 사라져서 더 헷갈린다.
- **해결**: `Sdf.Layer.FindOrOpen()` + `GetInfo('apiSchemas')`로 **원시 레이어를 직접** 조회.
  그러면 14개 스키마가 그대로 보인다.
- **참고**: GPU 없이 USD만 저작·조회할 때는 이 방법이 빠르다(앱 기동 불필요).
  `PYTHONPATH`/`LD_LIBRARY_PATH`에 `extscache/omni.usd.libs-*` 와 그 `bin/`을 넣고 `python.sh` 실행.

### 리프트 대상 슬롯 A5에 팀원 차량이 이미 주차돼 있음
- **원인**: 팀원 에셋이 A5에 `A5_Coupe`를 배치. 리프트/운반 테스트는 자기 Sedan을 같은 A5
  좌표 `(1.7, 0, 7.8)`에 스폰 → 차량 2대가 겹쳐 스폰.
- **해결**: 테스트 타깃을 빈 칸 **A7 `(8.5, 0, 7.8)`** 로 이동(슬롯 기하 동일, 같은 A열이라 비교 가능).
- **팀원에게 전달 필요**: 팀원 에셋 자체에 모순이 있다. `/World/Navigation`의
  `parking:target = A5`인데 그 자리에 `A5_Coupe`가 있다. 빌더의 `TARGET_INDEX = 5` 잔재로 보인다.

### 천장 라이다 비활성화가 조용히 무력화됨
- **증상**: 오프라인인데도 온라인 SICK 에셋을 받으려 대기.
- **원인**: 테스트들이 `/World/Sensors/CeilingLidar`를 하드코딩해 껐는데, 팀원 빌더는
  `CeilingLidarWest`/`CeilingLidarEast` **2개**를 만든다. 이름이 안 맞아 `IsValid()`가 False가 되고
  **오류 없이 그냥 건너뛴다**.
- **해결**: 이름 하드코딩 대신 `/World/Sensors` 자식을 순회하며 `CeilingLidar` 접두사로 매칭.

### 바닥이 깜빡임
- **원인**: `fab_vehicles.usd`의 쇼룸 바닥 디스크 `Cylinder001`(반경 10.84 m, 두께 0)이
  **정확히 Y=0**에 있어 주차장 바닥 상면과 동일 평면 → Z-fighting.
- **해결**: 테스트 스테이지에서 비활성화(원본 미수정). `DriveGround`/`FabLighting`과 같은 처리.
- **주의**: 충돌체가 아니므로 **물리 떨림과는 무관**하다. 순수 렌더링 문제.

### 차량이 영원히 떨림 → 로봇까지 밀려나 리프트 실패 (핵심)
- **증상**: 테스트 Sedan이 계속 떨고, 로봇이 팔 전개 중 0.6 m 밀려나 **앞축을 들어버림**.
- **오진**: 처음엔 "메카넘 롤러는 passive라 팔 반작용을 못 버틴다 → 실물에서도 성립하는 설계 한계"로
  결론냈다. **틀렸다.**
- **진단**: 물체별 떨림을 수치화(`diag_jitter.py`). drift(순변위)와 path(이동경로 총합)를 따로 재서
  진동과 표류를 분리.

  | 대상 | settle path | 떨림비(path/drift) | max step/frame |
  |---|---:|---:|---:|
  | test/Sedan | 0.632 m | 19.0 → hold에서 **37.5** | 0.0156 m |
  | 팀원 차량 10대 | ~0.08 m | ~1.0 (정착 후 **0.00000**) | ~0.004 m |
  | robot/base_link | **0.0001 m** | 1.1 | 0.00001 m |

  로봇은 전혀 떨지 않았다. **떠는 것은 Sedan 하나**였고, 그 진동이 팔을 통해 로봇을 밀어낸 것.
- **원인**: **PhysX에는 원기둥 프리미티브가 없다.** 원기둥은 내부적으로 면진 볼록체로 근사된다.
  팀원 차량은 PhysX Vehicle 구동계를 유지해 **레이캐스트 서스펜션**으로 떠 있어 바퀴가 바닥에
  닿지 않지만, 테스트 Sedan은 구동계를 제거해서 **원통 충돌체가 실제 접촉체로 승격**된다.
  서스펜션 없는 1600 kg이 면과 면 사이를 계속 넘나들며 영구 진동.
- **부분 해결**: `--keep-drivetrain` 플래그 추가(구동계를 **끄는** 게 아니라 **그대로 살려둠**).
  Sedan hold 구간 path가 0.662 → **0.00000**, 리프트 테스트 arrival 오차 **0.6056 → 0.001147 m**,
  뒷축 상승 0.004779 → **0.026486 m**(앞축 −0.000665로 정상 접지). PASS.
- **미해결**: 운반 데모에 같은 플래그를 적용하면 파국(차량 Y 0.868 m 공중부양, −2.75 m 역주행)은
  사라지지만 **차가 지면에 붙어 있어 실제로 안 들린다**(파지 후 Y −0.018, 오히려 하강).
  실제 메카넘 구동이 **0.056 m**에서 막히고 나머지를 유도 폴백이 채운다(HANDOFF 기준선은
  "폴백 불필요 + 실제 구동 1.04 m"). 서스펜션 레이캐스트가 차를 지면에 묶고 있는 것으로 추정.
- **최종 해결 — `--sphere-wheels`**: 구동계는 제거한 채(차가 자유 강체로 남아야 실려 간다)
  **휠 충돌체만 원통 → 구로 교체**. 로봇이 이미 쓰던 방식 그대로다.

  처음엔 "구는 폭이 2.7배로 부풀어 부적합"이라며 기각했는데 **그 기각이 틀렸다.** 이유 둘:
  1. **로봇 구동휠도 똑같이 부풀어 있다** — visual cylinder r=0.060/len=0.048 vs collision
     sphere r=0.060(폭 0.120) = **2.5배**. 차량(2.68배)과 사실상 같은 비율인데 로봇은 잘 쓴다.
     같은 기준을 차량에만 들이댄 셈.
  2. **2.7배는 휠 중심 높이에서의 최악값**이다. 팔 롤러가 실제 작업하는 y=0.045 높이에서 구의 폭은
     `2·√(0.343²−(0.343−0.045)²) = 0.340 m`로 원통(0.256) 대비 **1.33배**에 그친다.

  즉 계산만으로 기각했고 실측하지 않은 것이 잘못이었다. 실제로 돌려보니 세 구성 중 최고였다:

  | | 원본(원통) | `--keep-drivetrain` | **`--sphere-wheels`** |
  |---|---:|---:|---:|
  | Sedan settle path | 0.632 m | 0.029 m | **0.00002 m** |
  | 리프트 뒷축 상승 | 0.004779 ✗ | 0.026486 (마진 6%) | **0.028959 (마진 16%)** |
  | 운반 실제 구동 | −2.75 ✗ | 0.056 (폴백 의존) | **1.031 (폴백 불필요)** |
  | 운반 차량 리프트 | 0.868 공중부양 ✗ | −0.018 (안 들림) | **+0.0295** |

  `--replay-smoke`로 2회 연속 PASS 확인. HANDOFF 기준선(실제 구동 1.04 m, 폴백 불필요)을 회복했다.
- **`--keep-drivetrain`은 리프트 전용 우회책으로만 남긴다.** 서스펜션 레이캐스트가 차를 지면에
  묶어 운반이 안 되고, 리프트가 (a) 타이어가 팔 롤러에 눌린 것인지 (b) 서스펜션 광선이 팔을 맞아
  얹힌 것인지도 불명확하다. `--sphere-wheels`에는 이 모호함이 없다(차가 순수 강체 접촉으로 들린다).
- **교훈**: 계산으로 기각하기 전에 한 번 돌려볼 것. 특히 **프로젝트가 이미 같은 선택을 하고 있다면**
  그 선례가 계산보다 강한 증거다.

### 통합 필드가 항상 비메카넘 로봇을 쓰고 있었음 (그리고 그 반대도 아니었음)
- **증상 아님**: 오류 없이 조용히 잘못된 에셋이 들어감.
- **원인**: `build_integrated_parking_field.py`가
  `ROBOT_USD = LOCAL if LOCAL.is_file() else INSTALLED` 형태인데
  **LOCAL과 INSTALLED가 서로 다른 에셋**이었다(베이스 vs mecha_roller). 이름은 "로컬본/설치본"이라
  같은 파일의 두 위치처럼 읽히지만 실제로는 **로봇 종류를 바꾸는 분기**.
  게다가 `PROJECT_ROOT = ROOT.parent`가 한 단계 얕아(`Isaac_envo`, 실제는 `isaacpjt`) LOCAL이 항상
  존재하지 않았고, 결과적으로 폴백이 타서 mecha_roller가 쓰이되 **절대경로로 구워지고** 있었다.
- **해결**: 폴백 제거, `PROJECT_ROOT`를 `ROOT.parent.parent`로 수정, 참조를 상대경로화.
  리프트 테스트·운반 데모 포함 **3개 스크립트 모두 mecha_roller + 상대참조**로 통일.
- **교훈**: 폴백은 "같은 것의 다른 경로"일 때만 안전하다. **다른 물건으로 대체되는 폴백은 버그다.**

### 차량 축거 측정이 회전된 차량에서 오측
- **증상**: SUV·Coupe·Minivan·Offroad 4종의 축거가 0.8~1.0 m, 윤거 0.5 m로 물리적으로 불가능하게 나옴
  (바퀴 폭 0.601 m인데 윤거 0.504 m면 좌우가 겹침).
- **원인**: **월드 좌표의 축정렬 차이**로 쟀는데 그 4종은 쇼룸에서 회전 배치돼 있었다.
  Sedan은 우연히 축에 정렬돼 있어 맞게 나온 것.
- **해결**: `GetLocalTransformation()`의 로컬 좌표로 측정. 전부 정상값(축거 2.344~3.594 m).
- **부수 확인**: Sedan 축거 실측 **2.951 m**(마커 설계에서 가정했던 2.7 m와 다름. 다만 마커 z=±2.5
  결론은 여유 1.02 m로 유지됨).


---

## 2026-07-20 (밤) — 2로봇 운반 기본 구성 확정

### 구동계를 '유지'하는 쪽이 정답이었다 (앞선 결론 정정)
- **앞선 오판**: 자체 스폰 Sedan 하나로만 보고 "구동계를 유지하면 서스펜션이 차를 지면에
  묶어 운반이 안 된다"고 결론냈다(실제 구동 0.056 m, 차량 미상승). **표본 1개짜리 일반화였다.**
- **재검증**: 팀원이 실제로 주차해 둔 차량(A5_Coupe)을 대상으로 하니 정반대였다.
  구동계 유지가 리프트 0.0849 m로 가장 좋았고(구동계 제거+구의 3배), 추종률 1.000.
- **원인**: 자체 스폰 Sedan은 Y=0.0에 놓여 서스펜션이 지면에 파묻힌 채 시작한다.
  팀원 차량은 Y=0.035에 놓여 정상 동작한다. 차이는 구동계가 아니라 **스폰 높이**였다.
- **조치**: 자체 스폰 Sedan 경로를 삭제하고, 대상은 항상 주차된 차량으로 고정.
  기본값을 구동계 유지로 뒤집었다(`--strip-drivetrain`으로 예전 동작).

### PhysX Vehicle 휠에는 Mesh 충돌체를 걸 수 없다
- **증상**: 휠 충돌체를 볼록체로 바꾸자 주차 차량 12대가 날아감(변위 2622 m).
- **1차 오류**: 시각 Mesh(`<Wheel>/Visual/<Mesh>`)에 CollisionAPI를 걸었더니
  `the sub prim with CollisionAPI applied has to be a direct child of the wheel attachment prim`.
  지오메트리를 `<Wheel>/CollisionMesh`로 복제해 직속 자식으로 만들어 해결.
- **그래도 폭발**: convexDecomposition 2622 m, convexHull 1627 m. 조각 개수 문제가 아니다.
- **분리 증거**: 같은 코드·같은 메시·같은 변환인데 **구동계를 뗀 차량은 정상**이고
  **구동계가 살아있는 차량만** 폭발한다. 즉 Mesh 충돌체와 PhysX Vehicle 서브시스템의 충돌이다.
- **결론**: `fab_vehicles.usd`에 볼록체를 굽는 계획 폐기. 구동계를 유지하면 애초에 필요 없다.

### 합격 판정이 실패를 통과시키고 있었다
- **증상**: B5_Pickup이 리프트 0.4 mm·추종률 0.033인데 `DEMO_PASSED=True`.
- **원인**: `passed`가 `total_moved`만 봤는데, 실제 구동이 실패하면 **유도 폴백이 차를 대신
  옮겨** 기준을 채운다. 폴백은 원래 "화면상 동작은 보이게" 하려는 장치인데 판정까지 통과시켰다.
- **해결**: `real_drive_no_fallback` / `car_actually_lifted`(≥0.02 m) /
  `car_followed_robot`(추종률 0.8~1.2)을 모두 요구. 불합격 항목을 콘솔에 출력.
- **교훈**: 폴백이 있는 테스트는 **폴백을 썼는지 자체를 합격 조건에 넣어야** 한다.

### 차종 일반화가 안 되어 있다 (미해결)
- A5_Coupe(축거 2.715): 리프트 0.0849, 추종률 1.000 — **PASS**
- B5_Pickup(축거 3.594, 타이어 반경 0.440): 리프트 **0.0004** — 팔이 아예 못 듦
- B3_Minivan(축거 3.196): 리프트 0.0584이나 운반 실패
- arm pivot X=±0.27과 roller 간격이 Sedan/Coupe급에 맞춰져 있는 것으로 보인다.
  HANDOFF 7절에 리스크로만 적혀 있던 항목이 구체적 수치로 확인됐다.

### standalone GUI에서 충돌체가 안 보이는 문제
- **원인**: `SimulationApp`으로 띄우면 물리 디버그 시각화가 꺼진 채 시작하고 뷰포트
  메뉴(눈 아이콘 → Physics)에도 항목이 나타나지 않는다.
- **해결**: carb 설정을 직접 켠다(`--show-colliders`).
  `SETTING_DISPLAY_COLLIDERS = VisualizerMode.ALL`(NONE=0/SELECTED=1/ALL=2),
  `SETTING_VISUALIZATION_COLLISION_MESH = True`(쿠킹된 실제 충돌 메시를 그림).
  둘 다 `omni.physx.bindings._physx`에 상수로 있다.

---

## 2026-07-20 (심야) — 깊이캠+메카넘 합친 에셋

### 합친 에셋이 구동 중 발사됐다 (해결)
- **증상**: 정지·정착은 정상(y=0.0001 고정)인데, 전진 명령을 주면 81스텝(0.44 m)까지
  멀쩡히 가다가 82스텝에 14 cm 튀고, 108스텝에서 **vy≈12 m/s 임펄스**로 발사돼
  완전한 탄도 운동에 들어간다(140스텝 후 y=+4.53 m).
- **틀린 가설 3개** (모두 실측으로 기각):
  1. "롤러가 PhysX에 등록되지 않았다" — **제 진단 코드 버그**였다. 계층이 한 단계
     얕은 걸 반영 못 해 `/World/Robot/base_link/roller_*`를 조회했는데 실제 경로는
     `/World/Robot/roller_*`. 바로잡으니 롤러는 정상 등록돼 있었다.
  2. "휠 프림에 회전이 걸려 조인트 프레임이 어긋난다" — 양쪽 다 회전 0.
  3. "`WHEEL_CENTERS` 하드코딩이 이 에셋과 안 맞는다" — 휠 위치·롤러 위치 완전 동일.
- **진짜 원인**: 합성된 로봇 서브트리의 **활성 충돌체 수가 다르다.**
  기존 메카넘 40개(롤러뿐) vs 깊이캠 69개(롤러 40 + 29개). 그중 **캐스터 휠 4개**가
  월드 y=0.000~0.056 으로 지면에 정확히 닿는다. 메카넘 롤러는 y=-0.011 로 지면보다
  낮게 오서링돼 있어, 둘이 동시 접지하면 PhysX가 모순된 접촉을 풀며 에너지를 쌓는다.
  이 에셋의 베이스가 캐스터 구동 설계라 남은 잔재다.
- **해결**: `build_depth_cam_mecha_asset.py`에서 캐스터 충돌체만
  `collisionEnabled=False`. 섀시·암 충돌체는 차량 리프트에 필요하므로 남긴다.
  결과: 140스텝 후 y=+0.0003 m, 횡드리프트 0.19 m -> 0.000 m.
- **교훈**: "왜 안 되나"를 프림 계층·조인트에서 찾다가 세 번 헛짚었다. 정상 에셋과
  **합성된 상태의 충돌체 목록을 전수 비교**한 게 한 번에 답을 냈다. 라이브러리
  스코프(`/colliders/...`)의 원본 좌표는 의미가 없다 — 반드시 시뮬레이션에 합성된
  서브트리를 봐야 한다.

### 마커 인식 0/5 -> 5/5 (해결)
- **증상**: 1단계 물리 통과 후 A2->A6 횡이동에서 293프레임 전부 검출 0개.
- **원인 1 — 기하**: 전방 카메라는 로봇좌표 (x=+0.924, z=+0.090)에서 하향 30도.
  지면이 보이기 시작하는 건 로봇 앞 0.96 m부터인데, 테스트가 로봇을 마커 **좌표
  위에** 스폰하고 횡이동시켰다. 마커는 항상 로봇 발밑, 구조적으로 시야 밖.
- **실측 (`probe_marker_view.py` 신규)**: 검출 가능 구간은 로봇 중심 앞
  **1.1~1.4 m 뿐**. 1.0 m 이하는 시야 아래, 1.6 m 이상은 마커가 눕다 못해
  세로 37 px 미만이 되어 실패. (1.1 m: 545x236 px / 1.4 m: 163x37 px)
  -> 마커 행에서 1.25 m 물러서서 주행하도록 수정. 0/5 -> 3/5.
- **원인 2 — 병진 드리프트**: 개루프 메카넘 횡이동이 13.6 m에 +Z로 0.44 m(3.2%) 밀린다.
  검출 창이 0.30 m뿐이라 A4부터 창 밖. -> 표준오프 유지 제어 추가. 3/5 -> 4/5.
- **원인 3 — 종료 지점**: 마지막 마커에서 딱 멈추면 검출 구간(마커 통과 직후)에
  못 들어간다. -> 0.8 m 오버슛. 4/5 -> 5/5.
- **원인 4 — 요 드리프트**: 종이동만 계속 0개였다. 요각을 재보니 13.6 m 횡이동에
  **-12.8도**(이후 -14.5도) 누적. 요각 e는 1.25 m 앞 지면 패치를 `1.25*tan(e)`
  = 0.31 m 옆으로 민다. 종이동은 x를 마커에 정확히 붙잡았는데, 요각이 있으면
  그게 오히려 어긋난 위치였다. -> 방위 유지 제어 추가. **PHASE2_OK=True**.
- **제가 틀렸던 것**: 검출 x가 마커 x보다 0.1~0.5 m 뒤에서 잡히는 걸 "렌더 지연"으로
  의심했는데, 전부 요 드리프트였다. 방위를 잡으니 검출이 마커 x에 중심을 맞췄다
  (A6: 5.03~5.28 vs 마커 5.10).
- **wz 부호**: Y-up 스테이지 + 로봇 로컬축 재배치가 겹쳐 추론하면 틀린다.
  1단계에서 `wz=+0.3`을 넣고 요각 변화를 재서 부호를 정한다(실측 -51.5도 -> 부호 -1).

### 최종 결과
- `hwia_depth_cam_mecha_roller.usd`: 롤러 40 + 롤러조인트 40 + 카메라 4, 캐스터 충돌 0
- 1단계: 정착 y=0.0001 / 드리프트 0.0000 / 횡이동 1.544 m(교차 0.011) / 전진 1.532 m(교차 0.015)
- 2단계: 횡이동 A2~A6 **5/5**, 종이동 통과 검출 성공

### 남은 설계 이슈 (미해결)
- **카메라가 너무 낮다**: 높이 0.090 m 때문에 검출 창이 1.1~1.4 m(폭 0.30 m)뿐이다.
  차선 간격 3.40 m 대비 여유가 없고, 개루프 드리프트만으로 창을 벗어난다.
  즉 **마커 기반 주행이 방위·병진 폐루프 제어를 전제로 한다** — 개루프로는 성립하지 않는다.
  카메라를 높이면 창이 크게 넓어진다. 팀원 에셋 수정 여부는 결정 필요.
- 2단계 실행 중 PhysX 스레딩 경고(`Concurrent API write ... during fetchResults`)가
  대량 출력된다. 결과에는 영향이 없어 보이나 원인 미확인.

---

## 2026-07-21 — M2/M3 측위 파이프라인 (ROS 2)

### M2 검출 노드 — CV/ROS 분리 + cv2 4.5.4 IPPE 버그
- `src/parkbot_aruco/` 신설(colcon 빌드 OK). CV 로직(`aruco_pose.py`)을 ROS 노드
  (`aruco_detector.py`)에서 분리 → 자세 복원 검증이 ROS/Isaac 없이 돈다.
- **cv2 4.5.4 IPPE_SQUARE 버그**: `solvePnPGeneric(..., IPPE_SQUARE)`가 정면·순수피치
  마커에서 항등행렬(재투영 68~102px)만 반환. ITERATIVE는 같은 코너에 0.4px. →
  IPPE로 두 해(모호성)를 받되 최적해 재투영 2px 초과면 ITERATIVE 폴백.
- **평면 마커 깊이 관측성**: 합성 자세복원 8케이스에서 횡오차<5mm·회전<2.3°인데
  깊이오차는 최대 26mm. 차선추종은 횡·yaw로 하므로 무관, 깊이는 M3가 융합으로 메움.
- M2 관문 `test_aruco_pose.py` **8/8 통과**.

### M3 측위 — 좌표 규약이 첫 시도에 맞았다
- `marker_localizer.py`: `T_world_robot = T_world_marker·inv(T_cam_marker)·inv(T_base_cam)`
  + 오도메트리 융합 필터.
- **규약을 손으로 추론하지 않았다.** m3_localize_demo.py 가 marker→world 회전을
  {0,90,180,270} 스윕해 GT와 맞는 값을 고름 → **align_yaw=0** (계획서 0절 규약이 맞음).
- `T_base_cam`은 에셋에서 직접 읽음(손 타이핑 금지). 실제 Isaac 렌더 1프레임 E2E:
  마커 단독 측위 오차 **0.6cm / 0.3°**.

### M3 주행 융합 — 메카넘 오도메트리는 신뢰 불가 (마커가 필수인 이유)
- `m3_drive_localize_test.py`: A2→A7 17m 횡주행, 마커 인식마다 ID·좌표 로그.
  **6/6 인식**. 오도메트리만 최종 864cm 드리프트 vs 마커융합 18cm.
- **오도메트리 모델링에서 연속으로 헛짚음(각 원인 실측으로 규명):**
  1. 관절 '각도' 차분 → 연속 회전이 wrap 되어 구간 캘리브 27.9x 폭주. → '각속도' FK로 전환.
  2. 캘리브를 가속 전이 구간에서 함 → FK 부풀려짐. → 정상상태에서 측정.
  3. 개루프 순수 횡이동은 로봇이 yaw로 휘어 GT_x<경로길이 → 스케일 0.56x 오판.
     → 실주행과 같은 yaw 보정 조건에서 캘리브(0.907x).
- **남은 사실**: 복합운동(횡+전후보정 동시)에서 단순 메카넘 FK가 횡을 ~0.5x로 과소계산.
  이건 버그가 아니라 **메카넘 휠 오도메트리의 실제 한계**(롤러 횡슬립). 로봇은 물리적으로
  차선에 잘 붙어 있으나(GT z 안정) 바퀴로 계산한 '자기 위치 추정'이 9m씩 어긋난다.
  → **왜 절대 위치 마커가 필수인지를 수치로 증명**. FK 부호는 모듈 IK를 pinv로 뒤집어 유도.
- 리셋 오차 18cm(정지 단일측위 0.6cm보다 큼)은 빠른 드라이브바이 단일프레임 + 게인 0.9의
  잔여 10% 때문. 시야 내 다중프레임 보정이면 더 수렴.

---

## 2026-07-21 (오후) — M4 카메라 브링업 + M3 ROS 노드 (2-터미널 실동작)

사용자가 그린 구조를 실제로 구현·검증: **Isaac은 영상 토픽만 발행, 순수 ROS 노드가
받아 마커 검출+측위, ID·좌표 로그.**

### 만든 것
- `aruco_sim_bringup.py` (Isaac, M4): 전방 깊이캠 렌더 프로덕트 → OmniGraph
  `ROS2CameraHelper`(rgb) + `ROS2CameraInfoHelper` → `/image_raw`+`/camera_info` 발행.
  로봇을 A2→A7 주행(검증된 hold 보정). Isaac 안에서 rclpy 안 씀(C++ 브리지).
- `parkbot_aruco/marker_localizer_node.py` (순수 ROS, M3): image+camera_info 구독 →
  검출(aruco_pose) + 월드 측위(marker_localizer) → 마커 ID·월드좌표·로봇위치 로그.
  카메라 마운트 T_base_cam 은 파라미터(에셋 실측 기본값).

### 걸린 것
- **`ROS2CameraHelper`의 type 에 `camera_info` 없음** (허용: rgb/depth/segmentation…).
  "type is not supported" 에러. camera_info 는 별도 노드 `ROS2CameraInfoHelper` 를 써야 함.
- **도메인 불일치**: 브링업 기본 도메인 0 vs 노드 50 → 서로 발견 실패. 둘 다 같은
  `ROS_DOMAIN_ID` 로 맞춰야 함(이번엔 126). RMW=fastrtps + fastdds 화이트리스트 프로파일
  + LD_LIBRARY_PATH(브리지 humble lib) 는 [[ros2-isaac-bridge-setup]] 대로.

### 결과 (2-터미널 실동작 검증)
- 순수 ROS 노드가 Isaac 영상 토픽을 받아 **A2~A7 6/6 마커 전부** 로그:
  ID 1(A2)~6(A7), 각 월드좌표 + 로봇 측위. 재투영 대부분 <1px.
  로봇 측위가 GT 표준오프(z≈1.25)와 일치. "대기중" 로그 0 = 토픽 연결 즉시 성공.
- 예: `[측위] 마커 ID 2 (A3) 월드좌표=(-5.10,+2.50) → 로봇 x=-5.223 z=+1.246 yaw=-0.0° (재투영 0.83px)`

### 남은 것
- M6 폐루프 주행: 지금 주행은 Isaac 안 GT 기반 hold 보정(스크립트). 실제로는 노드가
  낸 측위로 /cmd_vel 을 돌리는 폐루프여야 함.
- vision_msgs 설치 시 /aruco/detections 정식 메시지 사용 가능(현재 노드는 자체 로그+PoseStamped).

### 주행 트리거 추가 — "토픽 한 번 pub → A2→A7" (실동작)
- 브링업에 `ROS2SubscribeTwist(/cmd_vel)` 노드 추가. 로봇은 A2 정지+카메라 발행 상태로
  대기하다가 `/cmd_vel` 에 한 번 pub 하면 A2→A7 주행 시작. (`--auto` 면 바로 주행)
- 실동작: 브링업 대기 → localizer 실행 → `ros2 topic pub --once /cmd_vel ...` →
  "/cmd_vel 수신 → 주행 시작" → A7 도착. 순수 ROS 노드가 A2~A7 6/6 마커 ID·월드좌표 로그.
  **사용자가 그린 흐름 그대로 성립.**

---

## 2026-07-23

### 운반 중 차 삐뚤(skew) — carry_to yaw프레임 가설은 기각, 진짜 원인은 heading 피드백 부재
- **증상**: 입차 운반 중 차가 처음 B칸(-z)쪽으로 살짝 쏠렸다가, 주차 후 차가 2~4° 틀어져 삐뚤.
  물리 240에선 잘 됐는데 120으로 낮추니 심해진다는 사용자 보고.
- **가설(기각됨)**: carry_to가 rear yaw 하나로 두 로봇에 같은 body 지령 → front yaw가 다르면
  같은 body 지령이 다른 world 방향이 돼 차가 회전. (carry_diverge.py 논리검증: 10° 드리프트
  가정 시 front가 -z 0.17 이탈.) → 로봇별 yaw로 수정.
- **headless 풀 E2E A/B 실측(물리 120, 슬롯 A2)**:
  - 수정 전(공유 yaw): 운반 중 baseline 흔들림 **3.1°**, 최종 92.3°, veh_z 최저 -0.36(B쪽 veer).
  - 수정 후(로봇별 yaw): 흔들림 **4.1°**, 최종 93.4°, veh_z 최저 -0.46.
  - → **유의미한 개선 없음(노이즈 수준).** 이 씬은 두 로봇 yaw 차가 작아 프레임 선택 효과 미미.
    가설의 전제(10° yaw 드리프트)가 실제로는 성립 안 함.
- **진짜 원인**: carry_to가 `_pub(r, vx, vy, 0.0)` — **wz=0, 차 heading 피드백이 전혀 없다.**
  두 로봇이 차 위치 오차(ex,ez)만 보고 동일하게 밀 뿐, 파지·마찰 비대칭이 만든 차 회전을
  아무도 보정 안 함 → 운반 중 heading이 단조로 90°→94° 드리프트(오실레이션 아님=외란 누적).
  물리 120에서 심해지는 것도 스텝 성김이 이 외란을 키우기 때문(제어 로직 문제 아님).
- **처리**: 물리 240 복원. CARRY_SHARED_YAW A/B 토글 제거하되 로봇별 yaw는 유지(더 올바른
  일반화, 무해). **진짜 해법은 미적용** — 가상중심 협조제어(설계노트 a37a1fc): carry_to에
  차 heading P제어(두 로봇 차분 스트래이프로 formation 회전 보정) 추가 필요. 사용자와 상의 후 진행.

### 운반 heading 가상강체 폐루프 적용 — 최종 오차 1° 이내 2회 재현
- **기준 재현(240Hz, A2 풀 E2E)**: 기존 `wz=0` 제어는 작업 자체는 `DONE`이지만 운반 시작
  대비 차량 최종 heading 오차 **-3.764°**, 최대 **3.764°**.
- **구현**:
  - runner가 `/vehicle/pose.orientation`에 차량 로컬 +Z 길이축의 월드 heading을 발행.
  - `carry_to`가 운반 시작 heading을 고정하고 차량 quaternion을 피드백(없으면 로봇 baseline 폴백).
  - heading P출력 `omega`를 두 로봇에 같은 자전으로만 주지 않고, 가상중심 기준
    `V_i = V_center + omega × r_i` 접선속도까지 분배해 하나의 강체 운동으로 만듦.
  - 위치와 heading(<0.5°)이 모두 수렴해야 leg 종료, 최종 1° 초과 시 액션 실패.
- **게인**: `Kp=0.8`, `omega_max=0.10rad/s`, deadband=0.10°, leg tol=0.50°.
- **GPU Isaac 240Hz A2 풀 E2E 결과**(동일 설정 2회, 모두 `DONE`):
  1. 차량 최대 **0.555°**, 최종 **+0.066°**, formation 최대 **0.700°**,
     baseline 길이 최대 변화 **0.0159m**.
  2. 차량 최대 **0.605°**, 최종 **-0.128°**, formation 최대 **0.767°**,
     baseline 길이 최대 변화 **0.0155m**.
- **판정**: 두 번 모두 운반 전 구간과 최종값이 1° 이내이며 baseline 변형도 1.6cm 이내.
  추가 게인 상승은 접촉 진동 여지만 늘리므로 현 설정으로 확정.
